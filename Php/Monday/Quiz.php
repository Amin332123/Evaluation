<?php 

// Quiz de validation — Cochez la bonne réponse

// Q1. En PHP, toute variable commence par : $ 

// Q2. Quelle est la différence entre une classe et un objet ? => La classe est le plan/moule, l'objet est l'instance concrète créée avec new

//  Q3. À quoi sert __construct() ? =>  A initialiser les attributs lors de la création d'un objet avec new

//  Q4. Que fait $this->nom = $nom; dans le constructeur ? => Assigne la valeur du paramètre $nom à l'attribut nom de l'objet courant

//  Q5. Comment appelle-t-on la méthode parler() sur l'objet $animal1 ? => $animal1->parler();